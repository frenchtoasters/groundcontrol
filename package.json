{
  "name": "@frenchtoastman/groundcontrol",
  "version": "0.1.17",
  "description": "OpenCode plugin that enforces provider allowlists and auto-exports sessions.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && bun build src/index.ts --outdir dist --format esm --target node --external @opencode-ai/plugin --external @opencode-ai/sdk --external tar --external unzipper",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "tar": "^6.2.0",
    "unzipper": "^0.12.1"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "1.1.48",
    "@types/node": "^20.0.0",
    "@types/tar": "^6.1.13",
    "@types/unzipper": "^0.10.11",
    "typescript": "^5.4.0"
  }
}
